create schema for d_timestamp

make database wrapper class object so globally accessable


redshift
review distkey -> determines how data distributed across cluster
sortkey -> creates compound columns that's sorted to enable binary search
primary key -> key used to reference table on joins 

