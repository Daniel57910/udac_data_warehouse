populate fact table
put iam user in secrets file
review refactoring and complete refactoring in accordance with project rubric
review pickle files

REFACTORING:
make database wrapper class object so globally accessable



redshift
review distkey -> determines how data distributed across cluster
sortkey -> creates compound columns that's sorted to enable binary search
primary key -> key used to reference table on joins 


song_staging

songplay_id: serial
timestamp: log_staging
user_id.log_staging 
level.log_staging
d_song.song_id on title
d_artist.artist_id on artist_name
session_id.log_staging
location.log_staging
user_agent.log_staging

